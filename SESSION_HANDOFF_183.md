# Session Handoff — Session 183

**Turn type:** doc-only (intake + policy). No engine, no data-file, no tooling change. `index.html`
stays **v6.14**. Baseline `--fetch --data-turn` green **29/29** at open (113/113 assertions,
137 guarded files, all three repro rebuilds byte-for-byte).

## What this session was

GW published the MFM v1.1 points update. The assigned E23 build turn was deferred (see sequencing
below); the session became the v1.1 intake: capture policy settled with Ryan, 15 new source files
custody-checked in the private repo, the parser's inability to read the new layout confirmed
empirically, and the refresh arc opened as B87 → B88 → B89. Everything is recorded in **D274** — read
it in full before touching B87; the layout deltas and the intake policy live there.

## Findings that shape the next sessions

1. **The v1.1 layout breaks `mfm_points_parser.py`.** On the SM v1.1 file: 154 unit names found
   (25 missed — Rhino, Razorback, Drop Pod among them, so detection failure, not roster removals)
   and **zero costs parsed**; the v1_0 file parses 179/179. B87 is the fix; nothing derived from any
   v1.1 file can be trusted until it ships.
2. **The v1.1 capture is richer than v1_0** — each page carries its DETACHMENTS block (DP, force
   disposition, enhancement costs, "UPDATED" flags) inline. B87 parses it structurally; B88 uses it.
3. **Custody clean, naming not.** All 15 files present, non-empty, uniform layout; SM repo copy
   text-identical to the in-session sample; the six codex-less chapters confirmed priced inside the
   SM page. But the uploads are dot-named ("v1.1") and one carries a space ("MFM_Chaos Daemons") —
   Filenames accepted as-uploaded per D275 (dots, spaces) — no rename step.
4. **Sequencing: MFM refresh arc runs ahead of the E23 build.** Adoption can change vehicle rosters,
   which would stale D273's per-army Tank Ace pool counts; E23's build turn re-verifies them at open.

## Ryan actions outstanding

None.

## Files — changed this session (SHA-256 first 12)

- `40K_Decision_Log.md` — D274 appended — `a1c3879ab67b`
- `DECISION_INDEX.md` — D274 index entry — `4200e557a048`
- `OPEN_ITEMS_BACKLOG.md` — status updated to 14 open; B87/B88/B89 added — `98130140d094`
- `NEXT_SESSION_PROMPT.md` — overwritten for Session 184 (B87 tooling turn) — `8082b4e42ea4`
- `pipeline_manifest.py` — `SESSION_HANDOFF_183.md` appended to GUARDED — `0b33eaac7eb5`
- `pipeline_manifest.json` — regenerated by `--write` after all text finalized (hash not listed here:
  its content includes this handoff's own hash)
- `SESSION_HANDOFF_183.md` — this file (net-new by number; rolling document, not net-new by role)

No file in this list is GW-derived; all are safe for the public repo. The 15 v1.1 MFM captures are
GW-derived and live in the **private** repo only — never committed to the public repo.

## Backlog state at close

Open 14: B69, B70, B75, B85, B86, P2, P4, E23, B67b, E12, B17, **B87, B88, B89** (new). Resolved
this session: none. Next: B87 (Session 184).
