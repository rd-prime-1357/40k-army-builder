# SESSION HANDOFF 197

**Turn type:** data-only. B89's third migration (Chaos Daemons) shipped. `index.html` untouched, stays
v6.16. `rules_assertions.py` untouched (no Chaos Daemons points values were pinned; nothing to
reconcile).

## What happened
1. **Baseline opened green.** `./baseline.sh --fetch --data-turn`: 32/32 gates pass. Verified S196's
   hashes via `pipeline_manifest.json` (the authoritative source) rather than the handoff table; repo
   matched, confirming the S196 push.
2. **Resolved the S197 prompt's open mechanism question**, per its own recommendation: investigated
   Chaos Daemons before the larger Space Marines-chain question. Confirmed from D132/D216/the
   architecture doc that CD's Gen-1 root CSVs are hand-authored source, never regenerated by any
   parser — a direct hand-edit of the 6 known point values is the correct, precedented mechanism, not
   the prohibited "hand-edit output" pattern. A `mfm_points_parser.py`-against-CD path exists in
   principle (`FACTION_BY_MFM` already maps CD's files) but has never been run or validated against
   Gen-1 data — that's new tooling work, deferred as a future Gen-2 question, not attempted this turn.
3. **Both CD MFM source files hash-verified directly** against `source_manifest.json`, then diffed
   directly against each other before trusting `MFM_v1_1_Reconciliation.md`. Every ▲/▼ marker traced to
   a specific unit.
4. **Found the reconciliation report wrong on Chaos Daemons, twice**: 3 enhancement re-prices
   (Inescapable Eye, Infernal Puppeteer, Neverblade) are attributed to SCINTILLATING LEGION but actually
   belong to SHADOW LEGION per the raw source; and a PLAGUE LEGION "FORCE DISPOSITION(S) CHANGED" banner
   in the raw v1.1 text is not in the report — checked directly and confirmed unchanged (`TAKE AND HOLD`
   both versions), a text-extraction artifact, not a missed real change. Both items are `detachments.json`
   scope (migrated separately under B89, same as TS/DG), flagged for whoever picks that up, not touched
   this turn.
5. **Hand-edited exactly 6 rows** in the root `Unit_Points.csv`: Beasts of Nurgle (70→75), Bloodcrushers
   (180→190, 105→115, 190→210), Fluxmaster (80→70), Kairos Fateweaver (295→305), Lord of Change
   (300→320, 315→340), Shalaxi Helbane (340→315). File-level diff confirmed only those 6 lines changed.
6. **Full pipeline chain run** exactly as `units_repro_check.py` runs it (SM/DG/TS/CSM via their normal
   chains, CD via `convert_to_json.py` run directly against the project root, then `merge_factions.py`
   and the three post-processors). Confirmed all 15 non-CD armies came out byte-identical, all four
   merged lookups and `faction_taxonomy.json` byte-identical.
7. **Exactly 6 Chaos Daemons units differ, all confined to `points`**, matching the 6 hand-verified
   values exactly. No diff fell outside the points block.
8. **`rules_assertions.py` checked for pinned Chaos Daemons point values** on all six units — none
   exist. 118/118 still passes unmodified.
9. **`source_manifest.json` updated** — `Unit_Points.csv` hash changed to reflect the edit. This is a
   genuine custody gap: `Unit_Points.csv` is GW-derived, private-repo-sourced; Claude's token there is
   read-only, so the edited file exists only in this session's working area and Ryan needs to push the
   matching update to `rd-prime-1357-data-sources`.
10. **`units_repro_check.py` needed no code change** — unlike TS/DG, CD has no source-file-swap branch
    to update; the pipeline always reads the one root file, now edited in place, so the check picked up
    the new values automatically.
11. **`units.json` regenerated and committed** — full 16-army merged output from the real pipeline.
12. Decision log (D290) and backlog updated; manifest regenerated last with `--write`, then
    `pipeline_manifest.py --freshness-check` run as the final command.

## State
- Baseline: green at close (`--no-repo`; `repo_check` pending push). 29/29 local gates pass.
- `index.html`: untouched, **v6.16**.
- `rules_assertions.py`: untouched, 118/118 (no Chaos Daemons points-value assertions existed to
  reconcile).
- `Unit_Points.csv` (CD root): **hand-edited**, 6 rows changed. Needs Ryan to push the matching edit
  to the private `rd-prime-1357-data-sources` repo — see Ryan action required below.
- `units.json`: **regenerated** — Chaos Daemons' 6 units carry new points. All other 15 armies
  byte-identical to the prior committed file.
- `units_repro_check.py`: unchanged — CD's block already read the (now-edited) root file directly; no
  branch to swap.
- `source_manifest.json`: `Unit_Points.csv` hash updated (`56c4bd70fd9b` → `db693cfce455`, first 12).
- `OPEN_ITEMS_BACKLOG.md`: 19 open, unchanged count — B89 advanced (third migration) but doesn't close;
  still spans the remaining priority-order factions.
- `pipeline_manifest.json`: regenerated at close, 156 guarded files.
- `repo_check` will show drift until pushed: `units.json`, `source_manifest.json`,
  `40K_Decision_Log.md`, `DECISION_INDEX.md`, `OPEN_ITEMS_BACKLOG.md`, `pipeline_manifest.py`,
  `pipeline_manifest.json`, `NEXT_SESSION_PROMPT.md`, `SESSION_HANDOFF_197.md` (net-new). Note:
  `Unit_Points.csv` is a private-repo file and is never part of `repo_check`'s public-repo comparison.

## Ryan action required
1. **Push `Unit_Points.csv`'s edit to the private `rd-prime-1357-data-sources` repo** — 6 rows changed
   (see "What happened" #5 above for the exact before/after values, or diff against this session's
   `source_manifest.json` hash: `db693cfce455...`). Claude's token there is read-only and cannot do
   this. Until pushed, a future `--fetch --data-turn` open will hash-mismatch on this file.
2. Push this session's public-repo changes.
3. No file-list screenshot needed — nothing this session turned on project-area presence/absence.

## Decisions still waiting on Ryan
None outstanding. The CD mechanism decision (hand-edit vs. new tooling) was a dev-manager scope/process
call per the standing development-decision-authority rule, recorded here and in D290 rather than
surfaced. The two reconciliation-report corrections (SHADOW LEGION misattribution, PLAGUE LEGION banner
false-positive) are factual findings, not decisions — no product call attached, `detachments.json`
hasn't been touched.

## Files (SHA-256, first 12)
| file | sha256:12 | note |
|------|-----------|------|
| `units.json` | b5b6cd4f7750 | Chaos Daemons: 6 units' points updated |
| `Unit_Points.csv` | db693cfce455 | CD root: 6 rows hand-edited; needs push to private repo (Ryan) |
| `source_manifest.json` | 82bbd7413cf4 | `Unit_Points.csv` hash updated to match the edit |
| `40K_Decision_Log.md` | 14a2395ead4b | D290 appended |
| `DECISION_INDEX.md` | 0fb475514b86 | D290 index entry |
| `OPEN_ITEMS_BACKLOG.md` | 04d6129b5856 | B89 updated with S197 progress; 19 open |
| `pipeline_manifest.py` | af471127b5d2 | `SESSION_HANDOFF_197.md` registered in GUARDED |
| `pipeline_manifest.json` | regenerated at close | `--write`, 156 guarded files (final pass, includes this handoff) |
| `NEXT_SESSION_PROMPT.md` | (unguarded by design) | S198 (B89's fourth migration) |
| `SESSION_HANDOFF_197.md` | (this file) | net-new; hash banked in the manifest by `--write` |

## Backlog
19 open, unchanged from S197 open. Beginning: B99, B98, B97, E28, B93, B90, B94, B89, B85, B86, B69,
B70, B75, P2, P4, E23, B67b, E12, B17 (19). Resolved: none. Added: none. Ending: same 19 — B89 advanced
(Chaos Daemons migrated, third of the arc) but doesn't close; still spans the remaining priority-order
factions.
